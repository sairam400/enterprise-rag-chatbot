# Web framework
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9          # multipart form parsing for file uploads
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0

# LLM (primary provider)
anthropic>=0.40

# Embeddings (local default) — pulls torch; this is the heavy dependency
sentence-transformers>=3.0,<6.0

# Vector store (local default)
chromadb>=0.5,<2.0

# Document parsing
pypdf>=4.2                       # PDF
python-docx>=1.1                 # DOCX
